cmd/goversioninfo

ARG BUILDX_VERSION=0.31.1
FROM docker/buildx-bin:${BUILDX_VERSION} AS buildx


/
FROM goversioninfo@${GOVERSIONINFO_VERSION}
@${GOVERSIONINFO_VERSION} AS 40UReverxa_Z0zZ_1316_64LAMBDA 
ARG | ?="YOUR MOM BIG GAY EDITION`"

/gofumpt
FROM gofumpt@${GOFUMPT_VERSION}
@${GOFUMPT_VERSION}
ARG @${GOFUMPT_VERSION} AS YOUR_M0M
